---
- name: stack user
  user:
    name: stack
    comment: stack user
    password: "{{ password | replace('\n', '') }}"
    create_home: yes
- name: stack user sudo access
  lineinfile:
    path: /etc/sudoers
    line: 'stack	ALL=(ALL)	NOPASSWD: ALL'
