- name: define overcloud network
  virt_net:
    command: define 
    name: overcloud
    xml: '{{ lookup("template", "overcloud.xml") }}'
  become: yes
- name: start the network
  virt_net:
    name: overcloud
    autostart: yes
    state: active
  become: yes
